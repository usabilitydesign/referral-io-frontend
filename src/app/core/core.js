import angular from 'angular';
import uiRouter from 'angular-ui-router';
// import coreCtrlr from './app.controller';

let coreModule = angular.module('core', [
  uiRouter
])

// .controller('coreCtrlr', coreCtrlr);

export default coreModule;
